{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install Flask gspread oauth2client gunicorn"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:$PORT app:app",
    "restartPolicyType": "ON_FAILURE"
  }
}
