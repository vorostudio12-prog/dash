{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python3 -m pip install Flask gspread oauth2client gunicorn"
  },
  "deploy": {
    "startCommand": "python3 -m gunicorn --bind 0.0.0.0:$PORT app:app",
    "restartPolicyType": "ON_FAILURE"
  }
}
